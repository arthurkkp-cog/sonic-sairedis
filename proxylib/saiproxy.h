#pragma once

/**
 * @brief Proxy config.
 *
 * Optional. Should point to a config.ini/profile.ini which will contain
 * profile variables that should be put into service method table.
 */
constexpr const char* SAI_PROXY_KEY_CONFIG = "SAI_PROXY_CONFIG";
